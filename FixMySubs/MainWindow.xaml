<Window
    x:Class="FixMySubs.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FixMySubs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:x1="using:HelperLibrary.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <NavigationView x:Name="navMenu"
                        Grid.Row="1"
                        PaneDisplayMode="Auto"
                        IsBackButtonVisible="Collapsed"
                        SelectionChanged="NavigationView_SelectionChanged">
            <NavigationView.Header>
                <TextBlock Text="Fix My Subs" FontSize="20" FontWeight="Bold" Margin="12,0,0,0" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
            </NavigationView.Header>
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home Page" Tag="HomePage" Icon="Home" />
            </NavigationView.MenuItems>
            <Frame x:Name="contentFrame" Margin="12,12,12,12" />
        </NavigationView>
    </Grid>
</Window>
